---
// Simple tip button wrapper
export interface Props {
  recipientAddress: string;
  imageId: string;
  creatorName?: string;
  className?: string;
}

const { recipientAddress, imageId, creatorName, className } = Astro.props;
---

<div data-tip-button class={className} data-recipient={recipientAddress} data-image={imageId}>
  <button class="btn btn-sm btn-primary">💜 Tip</button>
</div>

<style>
  [data-tip-button] {
    display: inline-block;
  }
</style>