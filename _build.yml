# Cloudflare Pages Build Configuration
version: 1
build:
  environment:
    NODE_VERSION: "20"
  command: |
    npm install -g pnpm@10.10.0
    pnpm install --frozen-lockfile
    pnpm run build
  directory: .  # Build from root
  publish: apps/web/dist