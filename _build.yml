# Cloudflare Pages Build Configuration
version: 1
build:
  environment:
    NODE_VERSION: "20"
  command: |
    npm install -g pnpm@10.10.0
    pnpm install --frozen-lockfile
    cd apps/web
    pnpm build
  directory: apps/web
  publish: dist